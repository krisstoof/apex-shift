# Integration guide v3

Use `audio_manifest_v3_realistic.json`.

Recommended replacement over v2:
- Use all `*_real_*` clips for combat and creature profiles.
- Keep v2 only for UI/building sounds if you prefer more readable feedback.

For #44:
1. Assign `spear_swing_real_*` to player attack clips.
2. Assign `spear_hit_flesh_real_*` to melee hit clips.
3. Assign `bow_release_real_*` to bow release clips.
4. Assign `trap_snap_real_*` to trap trigger.
5. Assign Varnak clips to creature audio profile.
